/**
 * TODO / Tools
 *
 * 1- Husky
 * 2- github actions / workflows
 * 3- benchmarks
 * 4- set of packages
 * 5- sh, make scripts
 * 6- gulp
 * 7- testing (jest,chi,cypress,playwright)
 * 8- devcontainer
 * 9- turbo, nx
 * 10- template
 * 11- ts runner >> (tsc, swc, esbuild, )
 * 12- runtime (debo, bun)
 * 13- cli like chalk
 */

/**
 * Steps
 *
 * npm init --y
 * npm i -D typescript ts-node
 * npx tsc --init
 *
 *
 *
 * npm i -D @swc-node/register
 *  node -r @swc-node/register index.ts
 * npx tsc index.ts # this will compile ts to js
 * npx ts-node index.ts # this will run ts
 *
 *
 */
